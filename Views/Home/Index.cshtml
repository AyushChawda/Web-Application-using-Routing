@{
	ViewData["Title"] = "Index";
	// Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>@ViewData["Title"]</title>
	<style>
		body {
		font-family: Arial, sans-serif;
		background-color: #f0f0f0;
		margin: 0;
		padding: 0;
		}

		.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 20px;
		}

		h1 {
		color: #333;
		text-align: center;
		}

		p {
		color: #666;
		font-size: 1.2em;
		line-height: 1.6;
		text-align: center;
		}



	</style>
</head>
<body>
	<div class="container">
		<h1>Home </h1>
		<p>Welcome to the home page! This is where you can find the latest updates and information.</p>
		<div style="display:flex">

			<div >
				<h1>using ViweData</h1>
				@{
					foreach (var item in (string[])ViewData["data"])
					{
						<ul>
							<li>@item</li>
						</ul>
					}
				}
			</div>
			<hr />
			<div>
				@{
					foreach (var item in (List<String>)ViewData["userList"])
					{
						<ol>
							<li>
								@item
							</li>
						</ol>
					}
				}
			</div>
		</div>
		<hr/>
		<div>
			<h2>using ViweBag</h2>
			<div>
				<h2>@ViewBag.name</h2>

				<div>
					@{
						foreach (var item in ViewBag.list)
						{
							<li>@item</li>
						}
					}
				</div>
			</div>
		</div>

		<hr />
		@* <div> *@
		@* 	<h2>using TempData</h2> *@
		@* 	<div> *@
		@* 		<h2>@ViewBag.name</h2> *@

		@* 		<div> *@
		@* 			@{ *@
		@* 				foreach (var item in (List<string>)TempData["list"]) *@
		@* 				{ *@
		@* 					<li>@item</li> *@
		@* 				} *@
		@* 			} *@
		@* 		</div> *@
		@* 	</div> *@
		@* </div> *@
	</div>
</body>
</html>
